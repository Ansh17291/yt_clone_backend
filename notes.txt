File Structure for backend
src: 
    index (db connect)
    app (config, cookie, )
    constanst (enums, db-name)
db:
models :
controllers :
Routes: 
middlewares: 
utils :
more(optional)


---------- DB_ modeling ----------------------------

We first always start with data modeling , how the db should look like then move ahead


1. creat a file named .. user.model.js where we store the db model .. hence .model 
2. install mongoose and go ahead to write the models 
3. mostly consider every attribute of the database as an object 
4. When we want to reference a model to another we need 2 things .. 1. type and 2. ref 
e.g. type : mongoose.Schema.Types.ObjectId,
     ref : "Users"


There is one more type named enum :[" " , " " ,  " "] .. here the attribute can take any of the one value form the enum array 

------------------ connectDB ----------------------------
while connecting to DB always remember one thing is .. "DB is always in another continent" therefore use async and await 

Before node 20 version there was an issue in while using .env while therefore we had this module named "dotenv" using that we can load all the env variables easily .. there are 2 methods to use that ... if using version 20 or higher skip ..
1. using normal syntax - require("dotenv").config({path: "./env"}); 
fine but if we are following import wala syntax then consistency issue . 

2. Using that import wala syntax .. import dotenv from "dotenv"
dotenv.config({
    path : "./env"
})

after this we need to configure some run commands. 
1. goto package.json then in srcipts do 
"srcipts" :{
    "dev" : "nodemon -r dotenv/config experimental-json-modules src/index.js
}

DONE;



app.use for middleware or configuration settings


_________________________________________________________________________________________________________________________
now we have created three files in the src directory 

1. app.js -> initializes the express for backend code, we import important stuff like 
    1. express - for backend 
        some methods that we use here are :
        1. express.json ---  is used for the get the parsed data as json in the body 
        2. express.urlencoded --- this is used to parse the url's that are given , like when we search somehthing different browsers have different excryption for thier url's therefore to make it standard we have this 
        3. express.static("public") --- used to make static files easier to access to the users 
        4. app.use(cookieParser()) --- ofc configure the cookieParser when app is initialized.
    2. cors - cross origin so backend can be connected to frontend 
    3. cookieParser .... just in case we use cookies 


2. index.js 

this is the entry point of the backend , here we make use of the app that we created above not the one from the express , becz we already configured that app in the app.js file so we use that , 
here we do basic connection of backend , listen for errors etc


3. constansts.js 

a file that has basic constants names like db_name or port number or anything that we use frequently and we can store it in constants we when it is changes it happens automatically everywheere and we need not need to do it manually . 
_________________________________________________________________________________________________________________________

src/db/index.js 

here we connect to the backend 

_________________________________________________________________________________________________________________________

src/utils

1. ApiError.js 
here we just make a standard error file which we will use when we want to give some error 

2. ApiResponse.js :
here we just make a standard response file so that we don't need to look for what we need to send again and again so we just make a standard class to send things 

3. AsyncHandler.js :
this is a very useful file , basically it just helps us avoid writing try catch again and again (just see the down function in the asyncHandler.js file .. the try catch one). so what does it do it
suppose we have this like a route 
app.get("/users", async(req, res)=>{
    const users = await User.find();
    res.json(users);
})

here await User.find() is async fuc so it can throw error or time out therefore we wrap this in try and catch like this 

app.get("/users", async(req, res)=>{
    try{
        const users = await Users.find();

    }catch(err){
        res.json({
            error;
        })
        next(err) // pass to error handler. 

    }
})

writing this try catch is annoying again and again therefore we have this asyncHandler 

app.get("/users", asyncHandler(async(req, res)=>{
    const users = await User.find();
    res.json(users);
}))

here we not only handle the errors is they come but we reduce the number of line we write and make it cleaner , and ofc also avoid that try catch from being written again and again !.